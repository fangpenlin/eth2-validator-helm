apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eth2-validator.fullname" . }}-test-connection"
  labels:
{{ include "eth2-validator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "eth2-validator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
