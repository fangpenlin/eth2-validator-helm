apiVersion: v1
kind: Service
metadata:
  name: {{ include "eth2-validator.fullname" . }}
  labels:
    {{- include "eth2-validator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eth2-validator.selectorLabels" . | nindent 4 }}
